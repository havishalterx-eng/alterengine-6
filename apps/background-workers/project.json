{
  "name": "background-workers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/background-workers/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/background-workers"
      ],
      "options": {
        "commands": [
          "tsc -p apps/background-workers/tsconfig.app.json",
          "node apps/background-workers/scripts/copy-build-assets.mjs"
        ],
        "parallel": false
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": [
        "build"
      ],
      "options": {
        "command": "node dist/apps/background-workers/main.js"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "eslintConfig": "apps/background-workers/eslint.config.mjs",
        "lintFilePatterns": [
          "apps/background-workers/src/**/*.ts"
        ]
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p apps/background-workers/tsconfig.json"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run apps/background-workers/src/**/*.spec.ts"
      }
    }
  },
  "tags": []
}
