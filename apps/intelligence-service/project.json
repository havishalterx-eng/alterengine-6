{
  "name": "intelligence-service",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/intelligence-service/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "uv sync --frozen",
          "uv run python scripts/generate_protos.py --check"
        ],
        "parallel": false,
        "cwd": "apps/intelligence-service"
      }
    },
    "proto": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run python scripts/generate_protos.py --check",
        "cwd": "apps/intelligence-service"
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run uvicorn src.main:app",
        "cwd": "apps/intelligence-service"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run ruff check .",
        "cwd": "apps/intelligence-service"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run mypy .",
        "cwd": "apps/intelligence-service"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run pytest",
        "cwd": "apps/intelligence-service"
      }
    },
    "migrate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run alembic upgrade head",
        "cwd": "apps/intelligence-service"
      }
    },
    "migrate-rollback": {
      "executor": "nx:run-commands",
      "options": {
        "command": "uv run alembic downgrade -1",
        "cwd": "apps/intelligence-service"
      }
    }
  },
  "tags": []
}
